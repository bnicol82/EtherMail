# Google OAuth client ID for Gmail read sync (PKCE SPA — no client secret)
# Create at https://console.cloud.google.com/apis/credentials
# Enable Gmail API + add redirect URI: http://localhost:5173/
VITE_GOOGLE_CLIENT_ID=
